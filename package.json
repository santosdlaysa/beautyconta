{
  "name": "beautyconta",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npm-run-all --parallel dev:backend dev:web",
    "dev:backend": "npm --prefix backend run dev",
    "dev:web": "npm --prefix web run dev",
    "dev:mobile": "npm --prefix mobile run start",
    "build": "npm --prefix backend run build && npm --prefix web run build",
    "test": "npm --prefix backend run test",
    "lint": "npm --prefix backend run lint && npm --prefix web run lint",
    "typecheck": "npm --prefix backend run typecheck && npm --prefix web run typecheck",
    "install:all": "npm --prefix backend install && npm --prefix web install && npm --prefix mobile install",
    "db:migrate": "npm --prefix backend run db:migrate",
    "db:deploy": "npm --prefix backend run db:deploy",
    "db:studio": "npm --prefix backend run db:studio"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
